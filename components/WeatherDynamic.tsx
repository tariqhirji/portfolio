const WeatherDynamic = () => {
  return (
    <div className="card card_secondary w-full flex items-center justify-center">
      <p>The weather where I am currently:</p>
    </div>
  );
};

export default WeatherDynamic;
